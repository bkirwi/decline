package com.monovore.decline

import cats.data.NonEmptyList
import org.scalatest.{Matchers, WordSpec}

class UsageSpec extends WordSpec with Matchers {

  "Usage" should {

//    "handle no opts" in {
//      Usage.fromOpts(Opts.always(15)) should equal(NonEmptyList.of(Usage()))
//    }
//
//    "handle a single argument" in {
//      val usage = Usage.fromOpts(Opts.option[Int]("foo", "..."))
//      usage should equal(NonEmptyList.of(Usage(opts = List(Usage.Options.Required("--foo <integer>")))))
//    }
  }
}